{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean_build",
            "type": "shell",
            "command": "bash",
            "args": [
                "-c",
                "source /opt/ros/humble/setup.bash && rm -rf build install && colcon build"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}
